import 'package:flutter/material.dart';

class SelectContactReceipt extends StatelessWidget {
  const SelectContactReceipt({
    Key key,
    this.onTap,
    
  }) : super(key: key);
  final Function onTap;
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('afdaf'),
    );
  }
}